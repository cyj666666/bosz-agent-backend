-- =============================================================================
-- app_report_prompt 新增：风险要点总结（RULE_SUMMARY）—— 系统内置项
-- =============================================================================
-- 背景（客户 2026-09-24）：把「风险要点」三段式（开篇总述 + 收尾结论）的提示词
--       **从代码里挪进表** —— 改提示词不用改代码、不用重新打包发版
--       （代码侧 ReportPromptService 每次调用都重新查表，改完立即生效）。
--
-- 🔴 sceneType = 'SYSTEM' ⇒ **「系统管理 → 通用提示词管理」页面不展示、不可编辑**。
--    原因：该提示词正文里有三个与后端解析**强耦合**的标记，改坏了报告直接少内容：
--      #PICK#      选中清单行（对应 AgentReportContentProvider.PICK_MARK）
--      #TAIL#      开篇/收尾结论的分隔行（对应 TAIL_MARK）
--      最多 5 条    要点条数上限（对应 MAX_RISK_ITEMS）
--    ⇒ 只允许在库里改；改的时候这三个必须**原样保留**。
--
-- ⚠️ 代码兜底：表里查不到 / isEnabled='N' / systemPrompt 为空时，回落到
--    resources/report-prompt/RULE_SUMMARY.system.txt（ReportRuleSummaryPrompt）。
--    所以本脚本**没跑也不会报错**，只是回落成兜底文本。
--
-- ⚠️ 外网与行内**都要执行**本脚本（两边各一份库）。
--
-- ⚠️ 幂等做法：先用 promptCode 删除、再插入 ⇒ 可反复执行、不会撞唯一索引
--    uk_report_prompt_code。🔴 代价是**重跑会把该条提示词刷回本脚本里的版本**
--    （在库里手工改过的内容会被覆盖）—— 要保留现场改动就别重跑。
--
-- 约定：不使用 IF NOT EXISTS；不使用反引号 / ENGINE / CHARSET；
--       多行提示词文本**必须顶格写**（SQL 字符串里的换行与缩进会原样进库）。
-- =============================================================================

-- 第 0 步（可选）：先看现状
-- SELECT id, promptCode, promptName, sceneType, isEnabled FROM app_report_prompt ORDER BY promptCode;

-- -----------------------------------------------------------------------------
-- 第 1 步：覆盖写入 RULE_SUMMARY
-- -----------------------------------------------------------------------------
DELETE FROM app_report_prompt WHERE promptCode = 'RULE_SUMMARY';

INSERT INTO app_report_prompt (promptCode, promptName, sceneType, systemPrompt, userPromptTemplate, isEnabled, remark)
VALUES ('RULE_SUMMARY', '风险要点总结', 'SYSTEM',
'你是银行贷后检查报告的风险汇总助手。用户会给你一组已经判定命中的风险要点（每条含所在章节、规则名称、风险文案）。
请**严格按下面的三段式**输出。

🔴 **输出里只允许出现报告正文本身**：禁止出现任何格式标注、行号、段号或说明文字（例如不得出现「第 1 行」「第 2 段」「格式说明」这类字样），也不要输出「以下是」「输出如下」「如下所示」之类的引导语。

第一行必须以 #PICK# 开头，后面用 | 分隔你挑出的**最严重、最需要关注的最多 5 条**的**规则名称**；名称必须与素材里给的规则名称**逐字一致**，不得改写、不得漏字或加字。

接着用 <p> 输出一段 **200~300 字** 的开篇总述，依次讲清：① 本次贷后检查共识别风险点几个（**用素材里给的条数**）、主要集中于哪几个方面；② 其中哪 2~3 项是最突出的风险信号、建议优先处理；③ 客户经理应做的动作（核实真实情况及成因、逐项落实整改措施、评估对授信安全的影响、必要时启动授信策略重评或合同违约处理程序）。**这一段必须以「其中最突出的风险信息情况如下：」单独一句收尾。**
🔴 开篇总述必须是**连贯的整段归纳**：绝对**不要逐条罗列**要点，也**不得出现任何序号引导词**（例如「一是」「二是」「四是」「其十二是」这类说法、或者阿拉伯数字编号）；更**不得把规则名称与它的描述原文照抄进来**（例如不要写成「疑似假国资：借款人疑似假冒国企」的形式）——这些要点由正文的条目块单独渲染，你在总述里再列一遍就是重复，客户会视为缺陷。

然后单独占一行，只输出 #TAIL# 作为分隔。

最后用 <p> 输出一段 **80~150 字** 的收尾结论，必须以「综上，」开头：把上面挑出的那几项风险归纳成「分别指向什么问题」（如持续经营能力弱化 / 偿债结构恶化 / 第二还款来源削弱），点明已对银行授信安全构成何种压力，并给出总体管控建议（如尽快采取针对性措施、防范风险叠加共振）。措辞不要与开篇总述重复，同样**不得出现序号引导词、规则名称与原文照抄**。

不要臆造材料里没有的信息。',
'{material}',
'Y', '系统内置（sceneType=SYSTEM，不在通用提示词管理页展示）：风险要点三段式总结用；正文含 #PICK# / #TAIL# 解析标记与条数上限，改动前先看脚本头注释');

-- -----------------------------------------------------------------------------
-- 第 2 步：复核（应恰有 1 行，sceneType=SYSTEM，prompt_state=OK）
-- -----------------------------------------------------------------------------
-- SELECT id, promptCode, promptName, sceneType, isEnabled,
--        CASE WHEN systemPrompt IS NULL OR systemPrompt = '' THEN 'EMPTY' ELSE 'OK' END AS prompt_state
--   FROM app_report_prompt WHERE promptCode = 'RULE_SUMMARY';

-- -----------------------------------------------------------------------------
-- 临时停用（代码会自动回落到兜底资源文件，不影响报告生成）
-- -----------------------------------------------------------------------------
-- UPDATE app_report_prompt SET isEnabled = 'N', updateTime = CURRENT_TIMESTAMP
--  WHERE promptCode = 'RULE_SUMMARY';
