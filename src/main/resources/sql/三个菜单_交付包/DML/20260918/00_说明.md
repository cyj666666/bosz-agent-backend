# 三个菜单 · DML 本地库快照（20260918）

## 这是什么

把 `DML/` 下那批交付包数据 DML，**按本地库当前实际数据重新导了一份**，放在本目录，用于备份 / 对照 / 回灌。

## 快照口径

| 项 | 值 |
|---|---|
| 数据来源 | **本地库** `127.0.0.1:5432/bosz`，`currentSchema=as_agent`（**只读** `SELECT *` 导出） |
| 快照时间 | **2026-09-18 13:46:27** |
| 组织形式 | 按**源 DML 文件名 1:1 镜像**，每个文件只导它原本负责的那几张表 |
| 语句形式 | **只有 `INSERT`**（纯数据）—— 不含原文件里的 `UPDATE` / 校验 / 动态授权生成语句 |
| 分块 | 每 20 行拼一条 `INSERT`（仅为可读性与可 diff；重放不受影响） |
| 行序 | 每张表 `ORDER BY 第 1 列`（保证两次导出可比对） |

## 文件清单与行数

| 文件 | 表（行数） | 合计 |
|---|---|---|
| `01_数据_基础配置_字典.sql` | `sys_dict` 88、`sys_dict_item` 24 | 112 |
| `02_数据_指标配置.sql` | `index_base_group` 29、`index_params` 920、`index_relate_knowledge_info` 492 | 1441 |
| `03_数据_知识配置.sql` | `knowledge_base_group` 25、`knowledge_base_params` 84、`knowledge_base_version` 1、`knowledge_relate_index` 494、`knowledge_relate_input_param` 9 | 613 |
| `04_数据_智策引擎.sql` | `agent_rule` 42 | 42 |
| `06_授权生成.sql` | `sys_role_knowledge` 25、`sys_role_knowledge_output` 87 | 112 |
| `08_数据_结果校验模板_大模型评估.sql` | `prompt_verify_scene_info` 1、`prompt_verify_scene_relate_prompt_info` 1 | 2 |

## 🔴 与交付包的行数差异（导完顺手核对了，4 处）

核对方法：从原 DML 文件里按 `('id','value'` 解析元组，并用「解析条数 == 文件头声明条数」自校验（两项都对上了，所以差集可信）。

### 1. `index_base_group` 少 11 个分组、`index_params` 少 166 条 —— ⚠️ 最大的一处

原文件头声明 40 / 1086，本地只有 **29 / 920**。**本地缺的 11 个分组全部是「财务指标」那棵子树**（含根节点本身）：

```
- 2092915239063465985  caiwuzhibiao          财务指标        ← 根节点
- 2092935305607852034  jinglirun             净利润
- 2092937268256911362  liebiao               财报列表
- 2093249329948078082  xisoshoulilv          销售利率、净利率
- 2093261500656463873  yingshouzhangkuan     应收账款和其他应收款
- 2094669762413015042  shishouziben          实收资本
- 2094669864242327553  zichanfuzhailv        资产负债率
- 2094669956173082625  fuzhaikemu            重要负债科目
- 2094670099131740162  caiwuqitafengxin      其他-存货与应收账款
- 2094670428825006082  yingyeshouru          营业收入
- 2098316800539242497  zdyq                  自定义
```

**「只在本地、交付包没有」= 0** ⇒ 本地是交付包的**子集**，不是另建了一套。
这条需要你确认：**是有意删的，还是本地当初就没导全？**（财务指标那棵树在智策引擎的指标树选择器里会看不全）

### 2. `knowledge_base_params` 少 3 条、多 1 条

- 本地缺：`skrsyr`、`jyk-sfczxianzhiquanli`、`jyk-sfczxianzhidwwdcdy`
- 本地多：`IntelligentStrategyEngine`（= `09_数据_智策引擎AI分析知识库.sql` 的本地增量，**符合预期**）
- 86 − 3 + 1 = 84 ✔ 与本地行数自洽
- ✅ 已确认这 3 条**不被任何规则引用**（42 条规则的 `additional_analysis` 全部能查到配置）

### 3. `knowledge_relate_index` 493 → 494（本地多 1 条）

### 4. `sys_role_knowledge_output` 87 行 vs `knowledge_base_params` 84 条

授权表比配置多 3 行，**疑似上面那 3 条被删配置残留的孤儿授权行**（未逐条验证，需要的话我拉个差集给你）。

## 跳过的源文件及原因

| 源文件 | 为什么不导 |
|---|---|
| `00_基础配置_模板.sql` | 它是**模板**（值全是 `<...>` 占位符），不是数据。⚠️ 另外：真去导 `sys_data_source` / `large_model_config` 会把 **`db_password` 密文 + `api_key` 明文**写进文件 → **有意不导**。需要的话说一声，我单独导并且只留非敏感列 |
| `05_映射改写.sql` | `UPDATE` 改写脚本，不是数据本身 |
| `07_校验.sql` | 纯 `SELECT` 校验语句 |
| `09_数据_智策引擎AI分析知识库.sql` | 本地增量，它那 1 行数据**已包含在 `03` 的快照里**（分开导会在主键上撞车） |
| `10_修正_指标SQL企业过滤.sql` | `UPDATE` 修正，结果已体现在 `02` 的 `index_params` 快照里 |
| `贷后业务数据_本地初始化_DML.sql` | **按你的要求排除** |

## 重放注意

1. 先 `SET search_path = <schema>, public;`
2. 顺序：`01` → `02` → `03` → `06`（`06` 依赖 01~04 已导入 + 库里有 `role_code='admin'` 的角色）
3. 本快照是**数据**，**不替代** `05_映射改写.sql` —— 数据源 id、模型编码口径仍要按目标环境改写
